# Server-only YouCam API bearer token. Never prefix with VITE_.
YOUCAM_API_KEY=

# Temporary access control for the protected /youcam-spike engineering route.
# Use a long random value and store the same secret in Vercel project settings.
YOUCAM_SPIKE_TOKEN=
